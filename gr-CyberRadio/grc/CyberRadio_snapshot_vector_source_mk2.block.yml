# auto-generated by grc.converter

id: CyberRadio_snapshot_vector_source_mk2
label: '[CyberRadio] Vita/IQ Snapshot Vector Source MK2'
category: '[CyberRadio]/Vita and UDP Sources'
flags:
- throttle

parameters:
-   id: radioObj
    label: Radio Object
    dtype: raw
-   id: ip
    label: IP
    dtype: string
    default: 0.0.0.0
    hide: part
-   id: port
    label: Port
    dtype: int
    default: '11000'
-   id: block_size
    label: Block Size
    dtype: int
    default: '131072'
    hide: ${ 'part' if vlen == 1 else 'none' }
-   id: block_rate
    label: Block Rate
    dtype: int
    default: '10'
-   id: demod
    label: Demod samples?
    dtype: enum
    options: ['True', 'False']
    option_labels: ['Off', 'On']
    option_attributes:
        num: ['1', '1']
        type: [complex, float]
    hide: part

inputs:
-   domain: message
    id: control
    optional: true

outputs:
-   domain: stream
    dtype: ${ demod.type }
    vlen: ${ block_size }
-   domain: message
    id: status
    optional: true

templates:
    imports: import CyberRadio
    make: "CyberRadio.snapshot_vector_source_mk2(${radioObj}.name.lower(), ${ip},\
        \ ${port}, ${block_size}, ${block_rate}, ${demod})\nprint(\"%s = CyberRadio.snapshot_vector_source_mk2(%r,\
        \ %r, %r, %r, %r)\"%(\"$(id)\", ${ip}, ${port}, ${block_size}, ${block_rate},\
        \ ${radioObj}.name.lower()))\n  "

file_format: 1
