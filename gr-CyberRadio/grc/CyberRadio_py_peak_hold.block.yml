# auto-generated by grc.converter

id: CyberRadio_py_peak_hold
label: '[CyberRadio] Peak Hold'
category: '[CyberRadio]/DSP'

parameters:
-   id: vecLen
    label: Vector Size
    dtype: int
    default: int(2**10)
    hide: ${ 'part' if vlen == 1 else 'none' }
-   id: clearEnable
    label: Clear Max
    dtype: bool
    hide: part
-   id: otherArgs
    label: Other Args
    category: Misc.
    dtype: raw
    default: '{}'
    hide: part
-   id: debug
    label: Debug
    category: Misc.
    dtype: bool
    default: 'False'
    options: ['False', 'True']
    option_labels: ['No', 'Yes']
    hide: part

inputs:
-   domain: stream
    dtype: float
    vlen: ${ vecLen }

outputs:
-   label: max
    domain: stream
    dtype: float
    vlen: ${ vecLen }

templates:
    imports: import CyberRadio
    make: CyberRadio.py_peak_hold(${vecLen}, ${clearEnable}, ${otherArgs}, ${debug})
    callbacks:
    - clearMax(${clearEnable})

file_format: 1
