# auto-generated by grc.converter

id: CyberRadio_generic_radio_control_block
label: Generic Control Base Block
category: '[CyberRadio]/Radio Element Control'

parameters:
-   id: cmd
    label: Command
    dtype: string
    default: '""'
-   id: radioParam
    label: Radio Parameters (dict)
    dtype: raw
    default: '{"type":"ndr308","host":"ndr308","port":8617,"obj":None}'
    hide: part
-   id: otherArgs
    label: Other Arguments
    dtype: raw
    default: '{}'
    hide: part
-   id: debug
    label: Debug Flag
    dtype: bool
    default: 'False'
    hide: part

inputs:
-   domain: message
    id: control
    optional: true

outputs:
-   domain: message
    id: status
    optional: true

templates:
    imports: import CyberRadio
    make: CyberRadio.generic_radio_control_block(${radioParam}, ${otherArgs}, ${debug})
    callbacks:
    - send_command(${cmd})
    - set_otherArgs(${otherArgs})

file_format: 1
