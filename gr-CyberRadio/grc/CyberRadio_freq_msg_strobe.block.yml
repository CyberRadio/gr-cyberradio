# auto-generated by grc.converter

id: CyberRadio_freq_msg_strobe
label: '[CyberRadio] Freq. Scan Msg Strobe'
category: '[CyberRadio]/Misc'

parameters:
-   id: pause
    label: Pause
    dtype: bool
    default: 'False'
    hide: part
-   id: f1
    label: F1 (MHz)
    dtype: float
-   id: f2
    label: F2 (MHz)
    dtype: float
-   id: step
    label: Step (MHz)
    dtype: float
-   id: dwell
    label: Dwell (seconds)
    dtype: float
-   id: wrap
    label: Repeat scan
    dtype: bool
    default: 'True'
    options: ['False', 'True']
    option_labels: ['No', 'Yes']
-   id: fManual
    label: Manual Freq. (MHz)
    dtype: raw
    default: None
    hide: part
-   id: msgUnits
    label: Message Units
    dtype: real
    default: '1.0'
    options: [1.0e9, 1.0e6, 1.0e3, '1.0']
    option_labels: [GHz, MHz, kHz, Hz]
    hide: part
-   id: msgRes
    label: Message Resolution
    dtype: real
    default: 1.0e6
    options: [1.0e9, 1.0e6, 1.0e3, '1.0']
    option_labels: [GHz, MHz, kHz, Hz]
    hide: part
-   id: debug
    label: Debug Flag
    category: Misc.
    dtype: bool
    default: 'True'
    options: ['False', 'True']
    option_labels: ['No', 'Yes']
    hide: part

outputs:
-   domain: message
    id: freq
    optional: true

templates:
    imports: import CyberRadio
    make: "CyberRadio.freq_msg_strobe(\n      pause = ${pause}, \n      f1 = ${f1},\
        \ \n      f2 = ${f2}, \n      step = ${step}, \n      dwell = ${dwell}, \n\
        \      wrap = ${wrap}, \n      fManual = ${fManual}, \n      msgUnits = ${msgUnits},\
        \ \n      msgRes = ${msgRes}, \n      debug = ${debug}, \n       )"
    callbacks:
    - set_f1(${f1})
    - set_f2(${f2})
    - set_dwell(${dwell})
    - set_step(${step})
    - set_fManual(${fManual})
    - set_pause(${pause})

file_format: 1
