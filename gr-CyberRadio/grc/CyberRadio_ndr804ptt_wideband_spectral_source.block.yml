# auto-generated by grc.converter

id: CyberRadio_ndr804ptt_wideband_spectral_source
label: '[CyberRadio] NDR804-PTT WB Spectral Source'
category: '[CyberRadio]/Vita and UDP Sources/NDR804'

parameters:
-   id: radioParam
    label: Radio Param Dict
    dtype: raw
    default: '{"type":"ndr804-ptt", "host":"ndr308", "port":8617, "obj":None}'
    hide: part
-   id: index
    label: Tuner/DDC Index
    dtype: int
    default: '1'
-   id: enable
    label: Enable
    dtype: bool
    default: 'True'
-   id: rate
    label: Spectral Rate (0=Full, 1=100Hz, 2=10Hz)
    dtype: int
    default: '2'
-   id: rfFreq
    label: RF Freq (MHz)
    dtype: int
    default: '1000'
-   id: rfAtten
    label: RF Attenuation (dB)
    dtype: int
    default: '0'
-   id: rfFilter
    label: RF Filter
    dtype: int
    default: '1'
-   id: radioInterface
    label: Radio Ethernet Interface Index
    dtype: int
    default: '1'
-   id: dipIndex
    label: DIP Index
    dtype: int
    default: '-1'
    hide: part
-   id: localInterface
    label: Local Ethernet Interface
    dtype: string
    default: '"eth6"'
-   id: udpPort
    label: UDP Port
    dtype: int
    default: '11000'
-   id: iirAvgGain
    label: IIR Avg. Gain Exponent (2^-x)
    dtype: int
    default: '5'
-   id: otherDdcArgs
    label: Other DDC Args
    dtype: raw
    default: '{}'
    hide: part
-   id: otherTunerArgs
    label: Other Tuner Args
    dtype: raw
    default: '{}'
    hide: part

inputs:
-   domain: message
    id: rfFreq
    optional: true

outputs:
-   domain: message
    id: rfFreq
    optional: true
-   label: Out
    domain: stream
    dtype: float
    vlen: 3200
    optional: true
-   label: AVG
    domain: stream
    dtype: float
    vlen: 3200
    optional: true

templates:
    imports: import CyberRadio
    make: "CyberRadio.ndr804ptt_wideband_spectral_source(\n      radioParam=${radioParam},\n\
        \      index=${index}, \n      enable=${enable}, \n      rate=${rate},\n \
        \     rfFreq=${rfFreq},\n      rfAtten=${rfAtten},\n      rfFilter=${rfFilter},\n\
        \      radioInterface=${radioInterface},\n      dipIndex=${dipIndex},\n  \
        \    localInterface=${localInterface},\n      udpPort=${udpPort},\n      iirAvgGain=${iirAvgGain},\n\
        \      otherDdcArgs=${otherDdcArgs},\n      otherTunerArgs=${otherTunerArgs},\n\
        \       )"
    callbacks:
    - set_dipIndex(${dipIndex})
    - set_iirAvgGain(${iirAvgGain})
    - set_enable(${enable})
    - set_localInterface(${localInterface})
    - set_otherDdcArgs(${otherDdcArgs})
    - set_otherTunerArgs(${otherTunerArgs})
    - set_radioInterface(${radioInterface})
    - set_radioParam(${radioParam})
    - set_rate(${rate})
    - set_rfAtten(${rfAtten})
    - set_rfFilter(${rfFilter})
    - set_rfFreq(${rfFreq})
    - set_udpPort(${udpPort})

file_format: 1
