# auto-generated by grc.converter

id: CyberRadio_single_pole_iir_filter_ff
label: '[CyberRadio] Single Pole IIR'
category: '[CyberRadio]/DSP Functions'

parameters:
-   id: alpha
    label: Alpha
    dtype: real
    default: '1.0'
-   id: resetOnAlphaChange
    label: Reset Filter on Alpha change?
    dtype: bool
    default: 'True'
-   id: reset
    label: Reset Filter State
    dtype: bool
    default: 'False'
-   id: vlen
    label: Vec Length
    dtype: int
    default: '1'
    hide: ${ ('part' if vlen == 1 else 'none') }

inputs:
-   domain: stream
    dtype: float
    vlen: ${ vlen }
-   domain: message
    id: reset
    optional: true

outputs:
-   domain: stream
    dtype: float
    vlen: ${ vlen }
asserts:
- ${ vlen > 0 }

templates:
    imports: import CyberRadio
    make: |-
        CyberRadio.single_pole_iir_filter_ff(
            ${alpha}, 
            ${vlen}, 
            ${resetOnAlphaChange}
        )
    callbacks:
    - set_taps(${alpha})
    - reset(${reset})

file_format: 1
