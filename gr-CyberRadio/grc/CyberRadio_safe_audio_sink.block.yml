# auto-generated by grc.converter

id: CyberRadio_safe_audio_sink
label: '[CyberRadio] Safe Audio Sink'
category: '[CyberRadio]/Misc'
flags:
- throttle

parameters:
-   id: samp_rate
    label: Sample Rate (Hz)
    dtype: real
    default: 4e3
-   id: device_name
    label: Device Name
    dtype: string
-   id: ok_to_block
    label: OK to Block?
    dtype: enum
    default: 'False'
    options: ['True', 'False']
    option_labels: ['Yes', 'No']

inputs:
-   domain: stream
    dtype: float
    vlen: 1

templates:
    imports: import CyberRadio
    make: CyberRadio.safe_audio_sink(${device_name}, ${samp_rate}, ${ok_to_block})

file_format: 1
