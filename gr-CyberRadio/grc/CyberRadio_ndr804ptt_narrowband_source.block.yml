# auto-generated by grc.converter

id: CyberRadio_ndr804ptt_narrowband_source
label: '[CyberRadio] NDR804-PTT NB IQ Source'
category: '[CyberRadio]/Vita and UDP Sources/NDR804'

parameters:
-   id: radioParam
    label: Radio Param Dict
    dtype: raw
    default: '{"type":"ndr804-ptt", "host":"ndr308", "port":8617, "obj":None}'
    hide: part
-   id: index
    label: Index
    dtype: int
    default: '1'
-   id: enable
    label: Enable
    dtype: bool
    default: 'True'
-   id: idString
    label: ID String
    dtype: raw
    default: '"USER"'
-   id: rate
    label: DDC Rate Index
    dtype: int
    default: '0'
-   id: ddcFreq
    label: DDC Frequency (Hz)
    dtype: float
    default: '0.0'
-   id: tunerIndex
    label: Tuner Index
    dtype: int
    default: '1'
-   id: radioInterface
    label: Radio Ethernet Interface Index
    dtype: int
    default: '1'
-   id: dipIndex
    label: DIP Index
    dtype: int
    default: '-1'
    hide: part
-   id: localInterface
    label: Local Ethernet Interface
    dtype: string
    default: '"eth6"'
-   id: udpPort
    label: UDP Port
    dtype: int
    default: '11000'
-   id: testFilename
    label: Test Filename
    dtype: raw
    default: '""'
-   id: otherDdcArgs
    label: Other DDC Args
    dtype: raw
    default: '{}'
    hide: part

inputs:
-   domain: message
    id: control
    optional: true

outputs:
-   domain: message
    id: control
    optional: true
-   domain: stream
    dtype: complex
    vlen: 1

templates:
    imports: import CyberRadio
    make: "CyberRadio.ndr804ptt_narrowband_source(\n        radioParam=${radioParam},\
        \ \n        index=${index}, \n        idString=${idString},\n        enable=${enable},\
        \ \n        rate=${rate}, \n        ddcFreq=${ddcFreq}, \n        tunerIndex=${tunerIndex},\
        \ \n        radioInterface=${radioInterface}, \n        dipIndex=${dipIndex},\
        \ \n        localInterface=${localInterface}, \n        udpPort=${udpPort},\n\
        \        testFilename=${testFilename},\n        otherDdcArgs=${otherDdcArgs},\
        \ \n        )"
    callbacks:
    - set_ddcFreq(${ddcFreq})
    - set_dipIndex(${dipIndex})
    - set_idString(${idString})
    - set_enable(${enable})
    - set_localInterface(${localInterface})
    - set_otherDdcArgs(${otherDdcArgs})
    - set_radioInterface(${radioInterface})
    - set_radioParam(${radioParam})
    - set_rate(${rate})
    - set_tunerIndex(${tunerIndex})
    - set_udpPort(${udpPort})

file_format: 1
