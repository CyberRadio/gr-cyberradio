# auto-generated by grc.converter

id: CyberRadio_generic_group_control_block
label: '[CyberRadio] NDR DDC Group Control'
category: '[CyberRadio]/Radio Element Control'

parameters:
-   id: radioObj
    label: Radio Object
    dtype: raw
    hide: part
-   id: index
    label: Group Index
    dtype: int
    default: '1'
-   id: enable
    label: Group Enable
    dtype: bool
    default: 'True'
-   id: wideband
    label: Wideband Flag
    dtype: bool
    default: 'True'
-   id: ddcList
    label: DDC List
    dtype: int_vector
    default: '[]'
    hide: part
-   id: otherArgs
    label: Other Args (dict)
    dtype: raw
    default: '{}'
    hide: part
-   id: debug
    label: Debug Flag
    dtype: bool
    default: 'False'
    hide: part

inputs:
-   domain: message
    id: control
    optional: true

outputs:
-   domain: message
    id: status
    optional: true

templates:
    imports: import CyberRadio
    make: "CyberRadio.generic_group_control_block(\n          ${radioObj}, \n    \
        \      ${index}, \n          ${enable}, \n          ${wideband}, \n      \
        \    ${ddcList}, \n          ${otherArgs}, \n          ${debug}, \n      \
        \     )"
    callbacks:
    - set_enable(${enable})
    - set_ddcList(${ddcList})
    - set_otherArgs(${otherArgs})

file_format: 1
