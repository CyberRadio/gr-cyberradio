# auto-generated by grc.converter

id: CyberRadio_py_msg_strobe
label: '[CyberRadio] py_msg_strobe'
category: '[CyberRadio]/Misc'

parameters:
-   id: value
    label: MSG Value
    dtype: raw
-   id: rate
    label: Rate (Hz)
    dtype: real
    default: '1.0'

inputs:
-   domain: message
    id: msg
    optional: true

outputs:
-   domain: message
    id: msg
    optional: true

templates:
    imports: import CyberRadio
    make: CyberRadio.py_msg_strobe(${value}, ${rate})
    callbacks:
    - setValue(${value})
    - setRate(${rate})

file_format: 1
